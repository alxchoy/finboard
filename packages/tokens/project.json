{
  "name": "@finboard/tokens",
  "projectType": "library",
  "tags": ["scope:tokens"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm build:tokens",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
